# Minimal Trivy config for CI (security stage)
severity:
  - HIGH
  - CRITICAL
scan:
  skip-dirs:
    - .git
    - kind-kubeconfig
  skip-files:
    - platform-kind/layers/storage/values/csi-hostpath/csi-hostpath-plugin.yaml
