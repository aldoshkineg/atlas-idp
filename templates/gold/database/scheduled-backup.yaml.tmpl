apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: {{APP}}-db-weekly
  namespace: {{NAMESPACE}}
spec:
  schedule: "0 0 3 * * 0"
  backupOwnerReference: self
  cluster:
    name: {{APP}}-db