apiVersion: v1
kind: ResourceQuota
metadata:
  name: {{APP}}-quota
  namespace: {{NAMESPACE}}
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi
    persistentvolumeclaims: "2"
    pods: "10"
