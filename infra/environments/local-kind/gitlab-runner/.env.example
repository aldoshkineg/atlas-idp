# Copy to .env (gitignored): cp .env.example .env
# Tags / run-untagged — задаются в GitLab UI при создании runner

GITLAB_RUNNER_TOKEN=glrt-REPLACE_ME

# Optional: create runner via API (make runner-create-api)
# GITLAB_PAT=glpat-REPLACE_ME

GITLAB_URL=https://gitlab.com/
GITLAB_PROJECT_PATH=aldoshkineg/atlas-idp
RUNNER_NAME=atlas-idp-local
