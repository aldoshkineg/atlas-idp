# GitHub token for act (rate limit) / ghcr. Kept out of .env so it is NOT
# part of the ENV_FILE secret; act reads it from .secrets and passes it as a
# separate secret. Copy this file to .secrets and fill in the value.
GITHUB_TOKEN=
