apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-cert
  namespace: nginx-gateway-fabric
spec:
  secretName: grafana-cert
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - atlas-idp
  dnsNames:
    - grafana.atlas
  issuerRef:
    name: dev-ca-issuer
    kind: ClusterIssuer
