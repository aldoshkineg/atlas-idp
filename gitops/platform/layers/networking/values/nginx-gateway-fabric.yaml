service:
  type: NodePort

nodeSelector:
  ingress-ready: "true"

tolerations:
  - key: node-role.kubernetes.io/control-plane
    operator: Equal
    effect: NoSchedule

metrics:
  enable: true
  port: 9113

nginxGateway:
  kind: deployment
  gatewayClassName: nginx
  replicaCount: 1
