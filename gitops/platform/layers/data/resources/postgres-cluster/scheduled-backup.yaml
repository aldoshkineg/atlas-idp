apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: production-db-weekly
  namespace: database
spec:
  schedule: "0 0 3 * * 0"
  backupOwnerReference: self
  cluster:
    name: production-db
