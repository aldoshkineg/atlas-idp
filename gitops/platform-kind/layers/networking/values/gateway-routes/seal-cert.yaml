apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: seal-cert
  namespace: nginx-gateway-fabric
spec:
  secretName: seal-cert
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - atlas-idp
  dnsNames:
    - seal.atlas
  issuerRef:
    name: dev-ca-issuer
    kind: ClusterIssuer
