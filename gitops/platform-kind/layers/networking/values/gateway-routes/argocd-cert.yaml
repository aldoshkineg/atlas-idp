apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-cert
  namespace: nginx-gateway-fabric
spec:
  secretName: argocd-cert
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - atlas-idp
  dnsNames:
    - argocd.atlas
  issuerRef:
    name: dev-ca-issuer
    kind: ClusterIssuer
