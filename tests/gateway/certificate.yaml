---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: atlas-ca-issuer
spec:
  ca:
    secretName: atlas-ca-secret
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-ca-cert
  namespace: kube-system
spec:
  secretName: test-ca-cert
  duration: 2160h
  renewBefore: 360h
  subject:
    organizations:
      - atlas-idp
  dnsNames:
    - test-ca.atlas
  issuerRef:
    name: atlas-ca-issuer
    kind: ClusterIssuer
