apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: backup-test-pvc
  namespace: testing
  labels:
    app: backup-test
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-hostpath-sc
  resources:
    requests:
      storage: 100Mi
