apiVersion: "cilium.io/v2"
kind: CiliumNetworkPolicy
metadata:
  name: db-backup-test-minio-access
  namespace: minio
spec:
  endpointSelector: {}
  ingress:
    - fromEndpoints:
        - matchLabels:
            io.kubernetes.pod.namespace: db-backup-test
      toPorts:
        - ports:
            - port: "9000"
