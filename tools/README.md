# Tools

Local development tooling for the Atlas IDP platform.

- `ci/act-runner/` — act-based local CI runner (Docker image + build/run script)
- `ci/local-runner/` — local GitHub Actions runner (Docker Compose + setup script)
- `argocd-login.sh` — helper to authenticate with ArgoCD via CLI
