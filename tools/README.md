# Tools

Local development tooling for the Atlas IDP platform.

- `ci/` — act-runner (local CI workflow runner) and local GitHub Actions runner
- `argocd-login.sh` — helper to authenticate with ArgoCD via CLI
