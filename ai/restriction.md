# Restriction

Never perform any of the following actions without explicit user approval:

- git commit, git push, merge, or rebase;
- terraform apply, destroy, refresh, or import;
- restarting, stopping, or deleting Docker containers, services, or Kubernetes resources.

Before performing any such action, first present the planned changes and wait for explicit confirmation from the user.
